import React from "react";
import "firebase/auth";
import { Link} from "react-router-dom";

const Personalcabinet = () => {

  return (
    <div>
      <Link to="/">
        {/* <button className="" onClick={() => handleClick(email, password)}> */}
          {/* Sign Out */}
        {/* </button> */}
      </Link>
    </div>
  );
};

export default Personalcabinet;
